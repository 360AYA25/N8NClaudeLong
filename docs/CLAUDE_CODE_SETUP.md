# Claude Code Setup (VS Code Extension)

Настройка n8n-claude-only MCP сервера для Claude Code в Visual Studio Code.

## 🚀 Быстрый старт

### 1. Установить зависимости и собрать

```bash
npm install
npm run build
```

### 2. Конфигурация для Claude Code

Файл уже создан: [`.vscode/mcp.json`](../.vscode/mcp.json)

```json
{
  "n8n-claude": {
    "command": "node",
    "args": ["${workspaceFolder}/dist/mcp/index.js"],
    "env": {
      "MCP_MODE": "stdio",
      "DATABASE_PATH": "${workspaceFolder}/data/nodes.db",
      "LOG_LEVEL": "info"
    }
  }
}
```

**Важно:** Используется `${workspaceFolder}` - автоматически подставляется путь к проекту.

### 3. Перезагрузить VS Code

```
Cmd+Shift+P → "Developer: Reload Window"
```

### 4. Проверка

Откройте Claude Code в VS Code и напишите:

```
Покажи доступные MCP серверы
```

Должен появиться `n8n-claude` с 11 инструментами.

## 🔧 Конфигурация с n8n API

Для создания и управления workflow добавьте в `.vscode/mcp.json`:

```json
{
  "n8n-claude": {
    "command": "node",
    "args": ["${workspaceFolder}/dist/mcp/index.js"],
    "env": {
      "MCP_MODE": "stdio",
      "DATABASE_PATH": "${workspaceFolder}/data/nodes.db",
      "N8N_API_URL": "http://localhost:5678",
      "N8N_API_KEY": "your-api-key-here",
      "LOG_LEVEL": "info"
    }
  }
}
```

## 📝 Получение n8n API Key

1. Откройте n8n: `http://localhost:5678`
2. Settings → API
3. Generate new API key
4. Скопируйте в конфигурацию

## 🛠️ Доступные инструменты

### Без n8n API (только документация)
- ✅ `search_nodes` - Поиск нод
- ✅ `get_node` - Детали ноды
- ✅ `validate_node` - Валидация конфигурации
- ✅ `search_templates` - Поиск шаблонов
- ✅ `get_template` - Получить workflow JSON
- ✅ `validate_workflow` - Валидация workflow

### С n8n API (полный функционал)
- ✅ `create_workflow` - Создать workflow
- ✅ `get_workflow` - Получить workflow
- ✅ `update_workflow` - Обновить workflow
- ✅ `list_workflows` - Список всех workflow
- ✅ `execute_workflow` - Запустить workflow

## 🎯 Примеры использования

### Пример 1: Поиск нод

```
Найди все HTTP ноды в n8n
```

Claude Code автоматически использует `search_nodes` tool.

### Пример 2: Получить детали

```
Покажи все параметры ноды n8n-nodes-base.httpRequest
```

Claude использует `get_node` tool.

### Пример 3: Создать workflow (требует n8n API)

```
Создай workflow который:
1. Принимает webhook на /test
2. Делает GET запрос к https://api.github.com/users/sergey
3. Возвращает результат

Используй ноды: webhook и httpRequest
```

Claude автоматически:
1. Использует `search_nodes` для поиска нужных нод
2. Использует `get_node` для параметров
3. Использует `validate_workflow` для проверки
4. Использует `create_workflow` для создания в n8n

### Пример 4: Валидация перед созданием

```
Проверь этот workflow перед созданием:
{
  "name": "Test Workflow",
  "nodes": [...],
  "connections": {...}
}
```

## 🐛 Troubleshooting

### MCP сервер не появляется

**Проверка 1:** Проект собран?

```bash
ls -la dist/mcp/index.js
# Если файла нет:
npm run build
```

**Проверка 2:** `.vscode/mcp.json` существует?

```bash
cat .vscode/mcp.json
```

**Проверка 3:** VS Code перезагружен?

```
Cmd+Shift+P → "Developer: Reload Window"
```

### Database not found

```bash
# Создать директорию
mkdir -p data

# Запустить один раз для инициализации
npm start
# Ctrl+C для остановки
```

База данных создастся автоматически с примерами нод.

### n8n API connection failed

**Проверка 1:** n8n запущен?

```bash
curl http://localhost:5678/healthz
```

**Проверка 2:** API key правильный?

```bash
curl -H "X-N8N-API-KEY: your-key" \
  http://localhost:5678/api/v1/workflows
```

Если ошибка - проверьте API key в n8n Settings → API.

### Логи для отладки

Включите DEBUG режим в `.vscode/mcp.json`:

```json
{
  "n8n-claude": {
    "env": {
      "LOG_LEVEL": "debug"
    }
  }
}
```

Логи будут в VS Code Output → Claude Code.

## 📦 Структура проекта для Claude Code

```
N8NClaudeOnly/
├── .vscode/
│   ├── mcp.json          # MCP конфигурация (главный файл!)
│   └── settings.json     # VS Code настройки
├── src/                  # Исходный код
├── dist/                 # Собранный код (npm run build)
├── data/                 # База данных SQLite
└── docs/                 # Документация
```

## 🚀 Workflow для работы

### Шаг 1: Исследование
```
Какие ноды есть для работы с HTTP?
```

### Шаг 2: Детали
```
Покажи все параметры ноды httpRequest
```

### Шаг 3: Создание
```
Создай workflow с этими нодами:
- webhook (path: /test)
- httpRequest (url: https://api.example.com)
```

### Шаг 4: Валидация
```
Проверь созданный workflow на ошибки
```

### Шаг 5: Выполнение (если n8n API настроен)
```
Запусти workflow с ID: 123
```

## ⚡ Продвинутые возможности

### Кастомизация database

В `.vscode/mcp.json` измените:

```json
{
  "env": {
    "DATABASE_PATH": "/custom/path/to/nodes.db"
  }
}
```

### HTTP режим (для удаленного доступа)

В `.vscode/mcp.json`:

```json
{
  "env": {
    "MCP_MODE": "http",
    "PORT": "3000",
    "HOST": "0.0.0.0"
  }
}
```

Запустите:
```bash
npm start
```

Доступ: `http://localhost:3000/tools/search_nodes`

## 📚 Документация

- [Usage Guide](USAGE.md) - Детальное описание всех инструментов
- [Architecture](ARCHITECTURE.md) - Архитектура системы
- [README](../README.md) - Основная документация

## ✅ Готово!

Теперь Claude Code в VS Code может:
- ✅ Искать n8n ноды
- ✅ Показывать документацию
- ✅ Валидировать workflow
- ✅ Создавать workflow в n8n (с API)
- ✅ Управлять всем через естественный язык

**Просто откройте Claude Code в VS Code и начните работать!** 🎉
