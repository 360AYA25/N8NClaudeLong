# Session Init Guide - Длинные n8n Проекты

> Как правильно начать работу над существующим workflow который нужно доделать

---

## Шаг 0: Создать папку проекта

**ВАЖНО:** Все файлы, связанные с конкретным workflow, должны храниться в отдельной папке!

```bash
# Создать папку для проекта (имя = название workflow в lowercase)
mkdir -p projects/foodtracker

# Структура:
# projects/
#   foodtracker/
#     PROJECT_STATE.md     # Текущее состояние (ОБЯЗАТЕЛЬНО)
#     SUPABASE_SCHEMA.md   # Схема БД (если есть Supabase)
#     ARCHITECTURE.md      # Архитектура workflow
#     AI_PROMPT.md         # Промт AI Agent (с WHY аннотациями)
#     debug_log.md         # Debug сессии
```

**Зачем?**
- Изолирует работу над разными workflows
- Удобно переключаться между проектами
- Легко найти все файлы по конкретному workflow
- Source of truth для схемы БД и промптов

---

## Шаг 1: Создать PROJECT_STATE.md в папке проекта

В папке `projects/[workflow-name]/` создать файл с текущим состоянием:

```markdown
# Project State: [Название Workflow]

## Workflow Info
- **ID:** sw3Qs3Fe3JahEbbW
- **Name:** FoodTracker Bot
- **Nodes:** 56
- **Status:** В разработке

## Что работает
- [x] Telegram trigger
- [x] AI Agent базовая логика
- [x] Supabase подключение

## Что нужно доделать
1. Добавить Reply Keyboard
2. Исправить memory persistence
3. Настроить error handling

## Текущая проблема
[Описание что сейчас не работает]

## Последние изменения
- 2025-12-17: Добавил X узел
- 2025-12-16: Исправил Y

## Checkpoints (рабочие версии)
- v157: Базовая версия, всё работает
- v220: После добавления keyboard (сломано)
```

---

## Шаг 2: Инициализация сессии

### 2.1 Получить текущее состояние

```javascript
// Проверить версии workflow
n8n_workflow_versions({mode: "list", workflowId: "ID", limit: 5})

// Получить структуру (безопасно для больших workflow)
n8n_get_workflow({id: "ID", mode: "structure"})
```

### 2.2 Определить checkpoint

```javascript
// Запомнить последнюю рабочую версию
TodoWrite([
  {content: "Checkpoint: v157 (рабочая)", status: "completed", activeForm: "Saved"},
  {content: "Текущая задача: [описание]", status: "in_progress", activeForm: "Working..."}
])
```

### 2.3 Проверить Learning System

```javascript
// Шаг 1: Читаем INDEX (~500 tokens)
Read("learning/INDEX.md")

// Шаг 2: Находим строку для нужной категории
// Пример: "Telegram Bot" → Line 639

// Шаг 3: Читаем целевую секцию (~400 tokens)
Read("learning/LEARNINGS.md", {offset: 639, limit: 50})
```

### 2.4 Проверить схему БД (если работаем с Supabase)

```javascript
// Убедиться что SUPABASE_SCHEMA.md актуален
Read("projects/[workflow-name]/SUPABASE_SCHEMA.md")

// Опционально: сравнить с реальной БД
mcp__plugin_supabase_supabase__list_tables({project_id: "PROJECT_ID"})
```

---

## Шаг 3: Структура работы

### Для КАЖДОЙ задачи:

```
1. Записать в PROJECT_STATE.md что делаю
2. Сделать checkpoint (если меняю workflow)
3. Проверить learning/INDEX.md (может уже решал)
4. Если меняю БД → Read SUPABASE_SCHEMA.md
5. Если меняю AI prompt → Read AI_PROMPT.md
6. Сделать изменение
7. Validate
8. Если ошибка → записать в debug_log.md
9. Обновить PROJECT_STATE.md
```

### При ошибке (Anti-Loop):

```
Попытка 1: ❌ → записать в debug_log.md что пробовал
Попытка 2: ❌ → сравнить с попыткой 1, записать
Попытка 3: ❌ → СТОП! Read learning/INDEX.md, искать альтернативу
Попытка 4: ❌ → WebSearch site:community.n8n.io
Попытка 5: ❌ → WebSearch site:github.com/n8n-io
Попытка 6+: Спросить пользователя или rollback
```

---

## Шаг 4: Завершение сессии

### Обновить PROJECT_STATE.md:

```markdown
## Последние изменения
- 2025-12-17 15:30: Добавил Reply Keyboard через HTTP Request (L-100)
- 2025-12-17 14:00: Исправил binary data после IF (L-068)

## Checkpoints
- v230: После keyboard fix (РАБОТАЕТ)
- v157: Базовая версия
```

### После ПОДТВЕРЖДЁННОГО фикса обновить:

```javascript
// Если изменилась схема БД:
Edit("projects/[workflow-name]/SUPABASE_SCHEMA.md", ...)

// Если изменился AI prompt:
Edit("projects/[workflow-name]/AI_PROMPT.md", ...)

// Если изменилась структура workflow:
Edit("projects/[workflow-name]/ARCHITECTURE.md", ...)

// Если решена новая проблема:
Edit("learning/LEARNINGS.md", ...)
```

---

## Quick Commands

```bash
# Начать сессию
1. Read projects/[workflow-name]/PROJECT_STATE.md
2. n8n_workflow_versions list
3. Read learning/INDEX.md → targeted section

# Перед изменением БД
Read projects/[workflow-name]/SUPABASE_SCHEMA.md

# Перед изменением AI prompt
Read projects/[workflow-name]/AI_PROMPT.md

# Во время работы
- TodoWrite для tracking
- Validate после каждого изменения
- Записывать попытки в debug_log.md

# При ошибке (Escalation Protocol)
- Попытка 3: Read learning/INDEX.md + LEARNINGS.md
- Попытка 4: WebSearch site:community.n8n.io
- Попытка 5: WebSearch site:github.com/n8n-io
- Попытка 6: спросить или rollback

# Завершить сессию (ПОСЛЕ подтверждения фикса)
1. Update PROJECT_STATE.md
2. Update SUPABASE_SCHEMA.md (если БД изменилась)
3. Update AI_PROMPT.md (если prompt изменился)
4. Если новое решение → Add to learning/LEARNINGS.md
5. Commit changes
```

---

## Файлы проекта

```
/N8NClaudeLong/                      # Корень репозитория
├── CLAUDE.md                        # Главный промт + все протоколы
├── CHANGELOG.md                     # История версий
├── learning/                        # 🆕 Learning System
│   ├── INDEX.md                     # Индекс с номерами строк (~500 tokens)
│   ├── LEARNINGS.md                 # База знаний (1,500+ lines)
│   ├── CODE_EXAMPLES.md             # Примеры кода
│   └── N8N-RESOURCES.md             # Внешние ресурсы
├── Docs/                            # Документация
│   ├── SESSION_INIT_GUIDE.md        # Этот файл
│   └── PROCESS_FLOWCHART.md         # Визуальная диаграмма процессов
└── projects/                        # ПАПКА ДЛЯ WORKFLOW ПРОЕКТОВ
    └── foodtracker/                 # Проект FoodTracker (пример)
        ├── PROJECT_STATE.md         # Текущее состояние
        ├── SUPABASE_SCHEMA.md       # 🆕 Полная схема БД
        ├── ARCHITECTURE.md          # Архитектура workflow
        ├── AI_PROMPT.md             # 🆕 Промт AI Agent (с WHY)
        ├── debug_log.md             # Debug сессии
        ├── POST_MORTEM.md           # Анализ проблем
        └── ...                      # Другие файлы проекта
```

**Правило:** Один workflow = одна папка в `projects/`

---

## Key Documentation Files

| Файл | Когда читать | Когда обновлять |
|------|--------------|-----------------|
| `PROJECT_STATE.md` | Начало сессии | Каждое изменение |
| `SUPABASE_SCHEMA.md` | Перед изменением БД | После подтверждённого фикса |
| `AI_PROMPT.md` | Перед изменением prompt | После подтверждённого фикса |
| `ARCHITECTURE.md` | Перед изменением структуры | После подтверждённого фикса |
| `debug_log.md` | Начало debug сессии | После каждой попытки |
| `learning/INDEX.md` | Попытка 3+ | После нового решения |

---

*Главное: читать документацию ПЕРЕД изменениями, обновлять ПОСЛЕ подтверждения*
*Last Updated: 2025-12-22*
